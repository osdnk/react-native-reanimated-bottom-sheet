import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  handleFullHeader: {
    height: '100%',
    width: 0,
    position: 'absolute',
  },
  animatedContainer: {
    width: '100%',
    position: 'absolute',
    zIndex: 100,
  },
  handleLayoutHeader: {
    zIndex: 101,
  },
  container: {
    width: '100%',
  },
})
